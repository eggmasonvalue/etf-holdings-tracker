# Conventions

## Code Quality
Follow PEP-8 standards. Use `uv` for dependency management.

## State Management
State is managed locally in `state.json` using a fields-and-data approach to avoid redundant keys. The data is updated automatically via GitHub actions.
