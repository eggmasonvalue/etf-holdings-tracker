# ETF Holdings Tracker

Tracks daily share counts and weightings for specific tickers inside ETFs (MAPS and GRWG). Automatically compares fetched CSV data to local state, sending alerts via Discord webhook on changes, and saving updated state back to the repository via GitHub Actions.
